---
const { id, class: className, contain = true } = Astro.props;
console.log("HERO SECTION", id);
---

<section id="herosection">
	<div class={`${contain ? "container py-16 lg:py-8" : ""}`}>
		<slot />
	</div>
</section>
